contract @"Bet"(p1, p2, game, amt) = {
	for (@pmt1, win1, lose1 <- p1;
			 @pmt2, win2, lose2 <- p2) {
		match pmt1 == *amt and pmt2 == *amt {
			true => { 1 }
		}
	}
}
|
new myChannel in {
	@"Bet"!(*myChannel)
}
